export interface User {
    token: string;
    role: number;
    roles: string[];
}

export interface Account {
    id: string;
    fullName: string;
    userName: string;
    phone: string;
    email: string;
  }
